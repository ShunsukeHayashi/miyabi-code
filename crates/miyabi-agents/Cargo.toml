[package]
name = "miyabi-agents"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Miyabi Agents - Autonomous AI agents"
keywords = ["agents", "ai", "automation", "github", "cicd"]
categories = ["development-tools", "web-programming"]
readme = "../README.md"

[dependencies]
miyabi-types = { version = "0.1.0", path = "../miyabi-types" }
miyabi-core = { version = "0.1.0", path = "../miyabi-core" }
miyabi-github = { version = "0.1.0", path = "../miyabi-github" }
# miyabi-worktree = { version = "0.1.0", path = "../miyabi-worktree" } # Temporarily disabled due to Send issues
miyabi-llm = { version = "0.1.0", path = "../miyabi-llm" }
futures = "0.3"
miyabi-potpie = { version = "0.1.0", path = "../miyabi-potpie" }

tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = "0.3"
anyhow = "1.0"
async-trait = "0.1"
regex = "1.10"
reqwest = { version = "0.12", features = ["json"] }
octocrab = "0.47"

[dev-dependencies]
tempfile = "3.13"
