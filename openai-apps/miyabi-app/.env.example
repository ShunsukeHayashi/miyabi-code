# Miyabi MCP Server Configuration

# ===== Project Configuration =====
MIYABI_ROOT=/Users/shunsuke/Dev/01-miyabi/_core/miyabi-private
BASE_URL=http://localhost:4444

# ===== GitHub Configuration =====
GITHUB_TOKEN=your_github_token_here
MIYABI_REPO_OWNER=customer-cloud
MIYABI_REPO_NAME=miyabi-private

# ===== MCP Authentication (OAuth 2.1) =====
# Bearer token for MCP endpoint authentication
# Leave empty for development mode (auth disabled)
# Generate a secure token for production:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
MIYABI_ACCESS_TOKEN=

# ===== Server Configuration =====
# Port for the MCP server
PORT=8000

# ===== HTTPS Configuration (Required for Production) =====
# MCP spec requires HTTPS for OAuth tokens
# SSL_CERTFILE=/path/to/cert.pem
# SSL_KEYFILE=/path/to/key.pem
