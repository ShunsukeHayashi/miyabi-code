# Miyabi Context7 Cloud - Enterprise SaaS

Context7互換のスケーラブルなドキュメント検索SaaS基盤。
マルチテナント、自動スケーリング、高可用性対応。

## 🎯 特徴

| 機能 | 説明 |
|------|------|
| **Context7互換API** | `resolve-library-id`, `get-library-docs` |
| **マルチテナント** | Cognitoベースの認証・認可 |
| **自動スケーリング** | Lambda + Aurora Serverless v2 |
| **高可用性** | マルチAZ、CloudFront CDN |
| **セマンティック検索** | Bedrock Titan Embeddings + pgvector |
| **レート制限** | プラン別（Free/Pro/Enterprise） |
| **監視** | CloudWatch Dashboard + アラート |

## 🏗️ アーキテクチャ

```
                                    ┌─────────────┐
                                    │  CloudFront │
                                    │    (CDN)    │
                                    └──────┬──────┘
                                           │
┌──────────────────────────────────────────┼──────────────────────────────────────────┐
│                                          │                                          │
│  ┌───────────────┐    ┌─────────────────┴────────────────┐    ┌───────────────┐   │
│  │    Cognito    │◄───│           API Gateway            │───▶│     WAF       │   │
│  │  (Auth/JWT)   │    │     (REST + Rate Limiting)       │    │  (Security)   │   │
│  └───────────────┘    └─────────────────┬────────────────┘    └───────────────┘   │
│                                         │                                          │
│    ┌────────────────────────────────────┼────────────────────────────────────┐    │
│    │                          Lambda Functions                                │    │
│    │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                   │    │
│    │  │   Resolver   │  │    Query     │  │   Indexer    │                   │    │
│    │  │ (library ID) │  │  (semantic)  │  │ (embedding)  │                   │    │
│    │  └──────────────┘  └──────────────┘  └──────────────┘                   │    │
│    └────────────────────────────────────┬────────────────────────────────────┘    │
│                                         │                                          │
│    ┌───────────────────────────────────┼───────────────────────────────────┐     │
│    │                              Data Layer                                 │     │
│    │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐           │     │
│    │  │     Aurora     │  │    DynamoDB    │  │   ElastiCache  │           │     │
│    │  │  Serverless v2 │  │   (Metadata)   │  │    (Redis)     │           │     │
│    │  │  + pgvector    │  │                │  │                │           │     │
│    │  └────────────────┘  └────────────────┘  └────────────────┘           │     │
│    └───────────────────────────────────────────────────────────────────────┘     │
│                                                                                   │
│    ┌───────────────────────────────────────────────────────────────────────┐     │
│    │                         Async Processing                               │     │
│    │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐           │     │
│    │  │      SQS       │──│ Step Functions │──│    Bedrock     │           │     │
│    │  │    (Queue)     │  │   (Workflow)   │  │  (Embeddings)  │           │     │
│    │  └────────────────┘  └────────────────┘  └────────────────┘           │     │
│    └───────────────────────────────────────────────────────────────────────┘     │
│                                                                                   │
└───────────────────────────────────────────────────────────────────────────────────┘
```

## 💰 料金プラン

### Free
- 60リクエスト/分
- 10,000リクエスト/月
- 1ライブラリ

### Pro ($29/月)
- 600リクエスト/分
- 100,000リクエスト/月
- 無制限ライブラリ
- プライベートドキュメント

### Enterprise (要相談)
- 6,000リクエスト/分
- 無制限リクエスト
- SLA保証
- 専用サポート

## 📊 AWS コスト試算

| スケール | リクエスト/月 | 月額コスト |
|----------|--------------|-----------|
| Starter | 10K | ~$55 |
| Growth | 100K | ~$240 |
| Scale | 1M | ~$1,725 |
| Enterprise | 10M+ | ~$15,000+ |

## 🚀 デプロイ

### 前提条件

```bash
# AWS CLI設定
aws configure

# Node.js 18+
node --version

# CDK CLI
npm install -g aws-cdk
```

### デプロイ手順

```bash
# 1. リポジトリクローン
git clone <repo>
cd miyabi-context7-cloud

# 2. デプロイ実行
chmod +x deploy.sh
./deploy.sh prod

# 3. 出力確認
cat infrastructure/outputs.json
```

## 📁 プロジェクト構成

```
miyabi-context7-cloud/
├── README.md                    # このファイル
├── deploy.sh                    # デプロイスクリプト
├── infrastructure/              # AWS CDK
│   ├── bin/
│   │   └── app.ts              # CDKエントリポイント
│   ├── lib/
│   │   ├── auth-stack.ts       # Cognito認証
│   │   ├── data-stack.ts       # Aurora/DynamoDB/Redis
│   │   ├── compute-stack.ts    # Lambda/SQS/Step Functions
│   │   ├── api-stack.ts        # API Gateway/CloudFront/WAF
│   │   └── monitoring-stack.ts # CloudWatch
│   ├── cdk.json
│   ├── package.json
│   └── tsconfig.json
└── lambda/
    ├── api/
    │   ├── resolver.py         # ライブラリID解決
    │   └── query.py            # セマンティック検索
    ├── indexer/
    │   └── handler.py          # ドキュメントインデックス
    └── layers/
        └── shared/             # 共有依存関係
```

## 🔌 API仕様

### 認証

```bash
# APIキー認証（MCP/SDK用）
curl -H "x-api-key: YOUR_API_KEY" https://api.context7.miyabi.ai/v1/...

# JWT認証（ダッシュボード用）
curl -H "Authorization: Bearer YOUR_JWT" https://api.context7.miyabi.ai/v1/...
```

### エンドポイント

#### `POST /v1/resolve-library-id`

```json
// Request
{
  "libraryName": "react"
}

// Response
{
  "query": "react",
  "libraries": [
    {
      "id": "/facebook/react",
      "name": "React",
      "description": "A JavaScript library for building user interfaces",
      "chunks": 1542
    }
  ]
}
```

#### `POST /v1/get-library-docs`

```json
// Request
{
  "context7CompatibleLibraryID": "/facebook/react",
  "topic": "hooks useState",
  "tokens": 5000
}

// Response
{
  "library_id": "/facebook/react",
  "topic": "hooks useState",
  "content": "# useState\n\nuseState is a React Hook that...",
  "chunks_returned": 8,
  "approximate_tokens": 4850,
  "sources": ["docs/hooks-reference.md", "docs/hooks-state.md"]
}
```

#### `POST /v1/index`

```json
// Request (JWT認証必須)
{
  "library_id": "/my-company/internal-docs",
  "title": "Internal API Documentation",
  "content": "# Internal API\n\n## Authentication\n..."
}

// Response
{
  "status": "success",
  "library_id": "/my-company/internal-docs",
  "chunks_indexed": 45
}
```

### MCP互換エンドポイント

```bash
# ツール一覧
GET /mcp/tools/list

# ツール実行
POST /mcp/tools/call
{
  "name": "get_library_docs",
  "arguments": {
    "context7CompatibleLibraryID": "/vercel/next.js",
    "topic": "app router"
  }
}
```

## 🔧 MCP設定

### Claude Desktop / Cursor

```json
{
  "mcpServers": {
    "miyabi-context7": {
      "type": "http",
      "url": "https://api.context7.miyabi.ai",
      "headers": {
        "x-api-key": "YOUR_API_KEY"
      }
    }
  }
}
```

## 📈 モニタリング

デプロイ後、CloudWatch Dashboardで以下を監視:

- API リクエスト数・エラー率・レイテンシ
- Lambda 実行数・エラー・同時実行数
- Aurora CPU・接続数・ACU使用量
- Redis ヒット率・メモリ使用量

## 🔒 セキュリティ

- **WAF**: レート制限、一般的な攻撃防御
- **Cognito**: JWT認証、MFA対応
- **VPC**: プライベートサブネットでDB保護
- **暗号化**: S3/Aurora/DynamoDB全て暗号化
- **監査**: CloudTrail、API Gateway アクセスログ

## 🛠️ 運用

### スケールアップ

```bash
# Aurora ACU上限変更
aws rds modify-db-cluster \
  --db-cluster-identifier miyabi-context7-prod \
  --serverless-v2-scaling-configuration MinCapacity=2,MaxCapacity=128

# Lambda同時実行数変更
aws lambda put-function-concurrency \
  --function-name miyabi-context7-query-prod \
  --reserved-concurrent-executions 1000
```

### バックアップ

- Aurora: 自動バックアップ（30日保持）
- DynamoDB: PITR有効
- S3: バージョニング有効

## 📝 ライセンス

MIT License

---

**Miyabi Team** | [context7.miyabi.ai](https://context7.miyabi.ai)
