# Miyabi Desktop - Agent使い方ガイド

**最終更新**: 2025-11-02
**バージョン**: 2.0.0

---

## 🎯 概要

Miyabi Desktopは、Tmuxセッションで複数のAIエージェントを並列実行し、GitHub Issueの完全自動処理を実現します。

## 🚀 クイックスタート（4ステップ）

### Step 1: アプリ起動

```bash
cd /Users/shunsuke/Dev/miyabi-private/miyabi-desktop
npm run tauri dev
```

### Step 2: Tmux Manager を開く

左サイドバーから **"Tmux Manager"** タブをクリック

### Step 3: Refreshボタンをクリック

画面上部の **"Refresh"** ボタンをクリックして、エージェントリストを読み込み

### Step 4: エージェントを起動

好きなエージェントを選択して **"Start"** ボタンをクリック

**実行例の表示**:
```
=========================================
🚀 InfinityMode 起動中...
📅 開始時刻: 2025-11-02 00:15:30
📂 作業ディレクトリ: /Users/shunsuke/Dev/miyabi-private
📝 ログ保存先: .ai/logs/2025-11-02.md
⚙️  並列度: 3 | スプリントサイズ: 5
=========================================

[Miyabi実行ログがリアルタイムで表示される]

✅ 完了: 2025-11-02 00:45:30
```

---

## 🌊 推奨モード: Infinity Mode（完全自律実行）

### 特徴

- ✅ **完全自動** - Issue取得からマージまで全自動
- ✅ **並列実行** - 複数Issueを同時処理（デフォルト: 3並列）
- ✅ **自動レビュー** - 品質スコア≥80%で自動マージ
- ✅ **継続実行** - 全Issue完了まで自動継続

### 起動方法

1. Tmux Managerで **"InfinityMode"** を選択
2. **"Start"** ボタンをクリック
3. コーヒーを飲んで待つ ☕

### 実行内容

```bash
# Tmux セッションで実行されるコマンド
cd /Users/shunsuke/Dev/miyabi-private/miyabi-desktop
claude --dangerously-skip-permissions "cd /Users/shunsuke/Dev/miyabi-private && RUST_LOG=info cargo run --release --bin miyabi -- infinity --concurrency 3 --sprint-size 5"
```

**パラメータ説明**:
- `claude --dangerously-skip-permissions` - Claude Code CLI経由で実行
- `RUST_LOG=info` - ログレベルをINFOに設定（詳細ログ出力）
- `--concurrency 3` - 最大3個のIssueを並列処理
- `--sprint-size 5` - 1スプリント = 5 Issues

**可視化された出力**:
- 📅 **タイムスタンプ** - 開始・終了時刻を表示
- 📂 **作業ディレクトリ** - 実行場所を明示
- 📝 **ログ保存先** - `.ai/logs/日付.md` にログが保存される
- ⚙️ **パラメータ** - 並列度やスプリントサイズを表示

### ワークフロー

```
┌─────────────────────────────────────────────┐
│ 1. GitHub Issues 自動取得                    │
│    ├─ 優先度順にソート                       │
│    └─ ラベルでフィルタ                       │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 2. Issue → Task 分解（Coordinator Agent）    │
│    ├─ DAG構築                               │
│    └─ 依存関係解決                           │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 3. CodeGen Agent 起動（並列3個）              │
│    ├─ コード生成                             │
│    ├─ テスト実行                             │
│    └─ Clippy check                          │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 4. Review Agent 品質判定                     │
│    ├─ 静的解析                               │
│    ├─ セキュリティスキャン                    │
│    └─ 品質スコア算出                         │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│ 5. PR Agent 自動作成・マージ                  │
│    ├─ Conventional Commits                  │
│    ├─ 品質≥80% → 自動マージ                 │
│    └─ 品質<80% → Draft PR                   │
└─────────────────────────────────────────────┘
```

---

## ⚡ 並列実行モード

### 特徴

- 複数のIssueを同時に処理
- Infinity Modeより細かい制御が可能

### 起動方法

1. Tmux Managerで **"ParallelMode"** を選択
2. **"Start"** ボタンをクリック
3. 処理したいIssue番号を指定（オプション）

---

## 🎯 個別Agent モード（手動制御）

### 利用可能なAgent

#### 1. CoordinatorAgent（タスク統括）

**機能**:
- Issue → Task 分解
- DAG構築
- 依存関係解決

**起動方法**:
1. Tmux Managerで **"CoordinatorAgent"** を選択
2. **"Start"** ボタンをクリック
3. プロンプトでIssue番号を入力

```
Enter Issue Number: 214 [Enter]
```

#### 2. CodeGenAgent（コード生成）

**機能**:
- AI駆動コード生成（Claude Sonnet 4）
- 自動テスト実行
- Clippy check

**起動方法**:
同上（Issue番号必須）

#### 3. ReviewAgent（品質判定）

**機能**:
- 静的解析（cargo clippy）
- セキュリティスキャン（cargo audit）
- 品質スコアリング

**起動方法**:
同上（Issue番号必須）

---

## 📊 StatusMonitor（ステータス監視）

### 特徴

- リアルタイムでプロジェクトステータスを監視
- Worktree状態を表示
- Issue進捗を追跡

### 起動方法

1. Tmux Managerで **"StatusMonitor"** を選択
2. **"Start"** ボタンをクリック
3. リアルタイム更新が開始

---

## 🔧 トラブルシューティング

### ❌ エラー: "tmux is not installed"

**解決方法**:
```bash
brew install tmux
```

### ❌ エラー: "claude: command not found"

**解決方法**:
Claude Code CLI がインストールされていません。
```bash
# Claude Code CLI をインストール
npm install -g @anthropic-ai/claude-code

# または、公式サイトからインストール
# https://docs.claude.ai/docs/claude-code
```

### ❌ エラー: "Session already exists"

**解決方法**:
1. 既存のセッションを終了
   ```bash
   tmux kill-session -t miyabi-infinity
   ```
2. または、Tmux Managerで **"Kill"** ボタンをクリック

### ❌ エラー: "GITHUB_TOKEN not found"

**解決方法**:
1. `.env` ファイルを作成
   ```bash
   cd /Users/shunsuke/Dev/miyabi-private
   echo "GITHUB_TOKEN=ghp_your_token_here" > .env
   ```

### ❌ エラー: "No agents configured"

**解決方法**:
1. `.miyabi/agents.yaml` が存在することを確認
   ```bash
   ls -la /Users/shunsuke/Dev/miyabi-private/miyabi-desktop/.miyabi/agents.yaml
   ```
2. Tmux Managerで **"Refresh"** ボタンをクリック

### ❌ Startボタンを押しても何も表示されない

**解決方法**:
1. Tmuxセッションの出力を確認
   ```bash
   tmux attach -t miyabi-infinity
   # Ctrl+B → D で detach
   ```
2. または、Tmux Managerの **"View Output"** ボタンをクリック

### ❌ ログが表示されない

**解決方法**:
`RUST_LOG=info` が設定されているか確認
```bash
# .miyabi/agents.yaml を確認
grep "RUST_LOG" /Users/shunsuke/Dev/miyabi-private/miyabi-desktop/.miyabi/agents.yaml
```

---

## 📝 ログ確認

### エージェント実行ログ

```bash
# 最新ログを表示
cat /Users/shunsuke/Dev/miyabi-private/.ai/logs/$(date +%Y-%m-%d).md
```

### Tmuxセッション出力

Tmux Manager の各エージェント行にある **"View Output"** ボタンをクリック

---

## 🎓 推奨ワークフロー

### 初回セットアップ

1. **環境確認**
   ```bash
   miyabi status
   ```

2. **設定確認**
   ```bash
   cat miyabi-desktop/.miyabi/agents.yaml
   ```

3. **Tmux確認**
   ```bash
   tmux -V
   ```

### 日常運用

1. **朝**: Infinity Mode を起動
   - Tmux Manager → InfinityMode → Start

2. **昼**: ステータス確認
   - Tmux Manager → StatusMonitor → Start
   - または `miyabi status --watch`

3. **夕方**: レポート確認
   - `.ai/logs/` ディレクトリ内のログを確認

---

## 🚀 パフォーマンスチューニング

### 並列度の調整

```yaml
# .miyabi/agents.yaml を編集
command: |
  cd /Users/shunsuke/Dev/miyabi-private
  cargo run --release --bin miyabi -- infinity --concurrency 5  # 5並列に変更
```

### スプリントサイズの調整

```yaml
command: |
  cd /Users/shunsuke/Dev/miyabi-private
  cargo run --release --bin miyabi -- infinity --sprint-size 10  # 10 Issues/sprint
```

### 推奨値

| マシンスペック | concurrency | sprint_size |
|--------------|-------------|-------------|
| MacBook Pro M1 | 3 | 5 |
| MacBook Pro M2/M3 | 5 | 10 |
| Mac Studio | 8 | 15 |

---

## 📚 参考リンク

- [Miyabi README](../README.md)
- [Tauri Setup Guide](TAURI_SETUP.md)
- [Entity-Relation Model](../docs/ENTITY_RELATION_MODEL.md)
- [Label System Guide](../docs/LABEL_SYSTEM_GUIDE.md)

---

## ✅ チェックリスト

使い始める前に、以下を確認してください：

- [ ] tmux インストール済み (`tmux -V`)
- [ ] GITHUB_TOKEN 設定済み (`cat .env | grep GITHUB_TOKEN`)
- [ ] Miyabi ビルド完了 (`cargo build --release`)
- [ ] Tauri Dev サーバー起動中 (`npm run tauri dev`)
- [ ] `.miyabi/agents.yaml` 存在確認

---

**質問・フィードバック**:
- GitHub Issues: https://github.com/ShunsukeHayashi/Miyabi/issues
- Email: shunsuke@miyabi.dev

---

**Happy Coding with Miyabi! 🎉**
