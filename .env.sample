# Miyabi environment configuration sample
# Copy this file to `.env` and fill the required keys before running agents/tests.

# --- Core LLM providers ---------------------------------------------------
# Anthropic Claude (used by coordinator, review, etc.)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Groq (used by business agents fallback and test scripts)
GROQ_API_KEY=groq_your_key_here

# OpenAI (optional fallback for some LLM utilities)
OPENAI_API_KEY=sk-openai-your-key-here

# Google Gemini (optional LLM integration)
GOOGLE_API_KEY=your-google-api-key
GEMINI_API_KEY=your-google-gemini-key

# --- GitHub integration ----------------------------------------------------
# Required for `miyabi-cli` GitHub operations (issues/PRs)
GITHUB_TOKEN=ghp_your_personal_token

# --- Optional telemetry/monitoring keys -----------------------------------
# Add additional provider keys here as needed.
# AZURE_OPENAI_API_KEY=
# BEDROCK_API_KEY=


# Workflow state storage (optional override)
# MIYABI_WORKFLOW_STATE_DIR=~/.miyabi/workflows
