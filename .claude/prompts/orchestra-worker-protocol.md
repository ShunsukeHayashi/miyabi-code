# ============================================
# 🎼 Miyabi Orchestra Protocol - Claude Code Settings
# ============================================
# このファイルをClaude Codeの設定に追加してください
# ============================================

## セッション終了時の必須行動 (P0)

あなたがワーカー（%2, %3, %4, %5のいずれか）である場合、
セッション終了前に**必ず**以下のコマンドを実行してください：

```bash
tmux send-keys -t %1 '[WORKER-N セッション終了] 作業サマリ: 結果' Enter
```

## MCP ツール使用ガイド

### 報告送信（PUSH型・必須）
```bash
# Bashコマンドで直接送信
tmux send-keys -t %1 '[WORKER-N報告] 内容' Enter
```

### 利用可能なMCPツール

| ツール | 用途 | 使用例 |
|--------|------|--------|
| miyabi-tmux | ペイン間通信 | tmux_send_message |
| miyabi-rules | ルール検証 | miyabi_rules_validate |
| miyabi-obsidian | ドキュメント参照 | obsidian_read_document |
| miyabi-log-aggregator | ログ確認 | log_get_errors |
| miyabi-resource-monitor | リソース監視 | resource_overview |
| miyabi-git-inspector | Git状態 | git_status |

### セッションライフサイクル

1. **開始時**: オーケストレーターに開始報告
2. **作業中**: 進捗を適宜報告
3. **完了時**: 結果をPUSH報告
4. **終了時**: セッション終了報告（必須）

## 禁止事項

- ❌ オーケストレーターを確認しに行く（PULL禁止）
- ❌ 報告せずにセッション終了
- ❌ tmux capture-pane での他ペイン監視
