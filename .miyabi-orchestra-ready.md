# 🎭 Miyabi Orchestra - 使用準備完了

**セッション名**: `miyabi-orchestra`
**作成日時**: 2025-11-03
**ステータス**: ✅ 全Agent動作確認済み

---

## 📋 Pane構成

| Pane | Agent | 役割 | ターゲット指定 |
|------|-------|------|--------------|
| 0 | Conductor | あなた（指揮者） | `miyabi-orchestra:0.0` |
| 1 | カエデ | CodeGen - コード生成 | `miyabi-orchestra:0.1` |
| 2 | サクラ | Review - コードレビュー | `miyabi-orchestra:0.2` |
| 3 | ツバキ | PR - プルリクエスト | `miyabi-orchestra:0.3` |
| 4 | ボタン | Deployment - デプロイ | `miyabi-orchestra:0.4` |

---

## 🎯 セッションへの接続

```bash
# セッションに入る
tmux attach -t miyabi-orchestra

# セッションから抜ける（セッションは継続）
Ctrl-b d
```

---

## ⚡ クイックコマンド集

### 1. 全Agentに準備確認

```bash
# カエデ
tmux send-keys -t miyabi-orchestra:0.1 "echo '[カエデ] 準備OK！CodeGen担当です'" Enter

# サクラ
tmux send-keys -t miyabi-orchestra:0.2 "echo '[サクラ] 準備OK！Review担当です'" Enter

# ツバキ
tmux send-keys -t miyabi-orchestra:0.3 "echo '[ツバキ] 準備OK！PR担当です'" Enter

# ボタン
tmux send-keys -t miyabi-orchestra:0.4 "echo '[ボタン] 準備OK！Deploy担当です'" Enter
```

### 2. プロジェクト情報確認

```bash
# カエデに構成確認を依頼
tmux send-keys -t miyabi-orchestra:0.1 "ls -la crates/ | head -10" Enter

# サクラにGitステータス確認を依頼
tmux send-keys -t miyabi-orchestra:0.2 "git status" Enter

# ツバキに最近のコミット確認を依頼
tmux send-keys -t miyabi-orchestra:0.3 "git log --oneline -5" Enter

# ボタンにビルド確認を依頼
tmux send-keys -t miyabi-orchestra:0.4 "cargo --version && rustc --version" Enter
```

### 3. 実践タスク例: Issue処理

```bash
# カエデに実装を依頼
tmux send-keys -t miyabi-orchestra:0.1 "# Issue #270の実装をお願いします" Enter

# サクラにレビュー待機を依頼
tmux send-keys -t miyabi-orchestra:0.2 "# カエデの実装完了後、レビューをお願いします" Enter

# ツバキにPR準備を依頼
tmux send-keys -t miyabi-orchestra:0.3 "# レビュー完了後、PRを作成してください" Enter
```

### 4. 全Agentの状態確認

```bash
# Pane 0 (Conductor)
tmux capture-pane -t miyabi-orchestra:0.0 -p | tail -5

# Pane 1 (カエデ)
tmux capture-pane -t miyabi-orchestra:0.1 -p | tail -5

# Pane 2 (サクラ)
tmux capture-pane -t miyabi-orchestra:0.2 -p | tail -5

# Pane 3 (ツバキ)
tmux capture-pane -t miyabi-orchestra:0.3 -p | tail -5

# Pane 4 (ボタン)
tmux capture-pane -t miyabi-orchestra:0.4 -p | tail -5
```

### 5. 一括状態確認スクリプト

```bash
# 全Agentの最新出力を一度に確認
for pane in 0 1 2 3 4; do
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  case $pane in
    0) echo "Pane $pane: Conductor" ;;
    1) echo "Pane $pane: カエデ (CodeGen)" ;;
    2) echo "Pane $pane: サクラ (Review)" ;;
    3) echo "Pane $pane: ツバキ (PR)" ;;
    4) echo "Pane $pane: ボタン (Deployment)" ;;
  esac
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  tmux capture-pane -t miyabi-orchestra:0.$pane -p | tail -3
  echo ""
done
```

---

## 🎮 Pane操作（セッション内で）

| 操作 | コマンド |
|------|---------|
| Pane移動 | `Ctrl-b + 矢印キー` |
| Pane番号表示 | `Ctrl-b + q` (番号表示中に数字キーで直接移動) |
| Paneフルスクリーン | `Ctrl-b + z` (もう一度で戻る) |
| Paneサイズ調整 | `Ctrl-b + Ctrl-矢印キー` |
| セッションから抜ける | `Ctrl-b + d` |

---

## 📚 実践例: Issue #270を4人で処理

### ステップ1: タスク分担

```bash
# Conductorから全員に指示
tmux send-keys -t miyabi-orchestra:0.1 "# カエデ: Issue #270のRust実装をお願い" Enter
tmux send-keys -t miyabi-orchestra:0.2 "# サクラ: 実装完了後、cargo clippy でレビュー" Enter
tmux send-keys -t miyabi-orchestra:0.3 "# ツバキ: レビュー完了後、PRを作成" Enter
tmux send-keys -t miyabi-orchestra:0.4 "# ボタン: PR完了後、cargo test --all で検証" Enter
```

### ステップ2: 進捗確認

```bash
# 各Agentの進捗を確認
for pane in 1 2 3 4; do
  tmux send-keys -t miyabi-orchestra:0.$pane "# 進捗状況を教えてください" Enter
done
```

### ステップ3: 完了確認

```bash
# 全Agent一括確認
tmux send-keys -t miyabi-orchestra:0.0 "echo '=== 全Agent完了確認 ==='" Enter
for pane in 1 2 3 4; do
  tmux capture-pane -t miyabi-orchestra:0.$pane -p | tail -1
done
```

---

## 🔧 トラブルシューティング

### Agentが反応しない場合

```bash
# 該当Paneをクリア
tmux send-keys -t miyabi-orchestra:0.[PANE_NUM] C-c
tmux send-keys -t miyabi-orchestra:0.[PANE_NUM] "clear" Enter

# 再度指示
tmux send-keys -t miyabi-orchestra:0.[PANE_NUM] "[新しい指示]" Enter
```

### セッション全体をリセット

```bash
# セッションを終了
tmux kill-session -t miyabi-orchestra

# 再度セットアップ
cd /home/user/miyabi-private
tmux
./scripts/miyabi-orchestra.sh coding-ensemble
```

---

## 📖 詳細ドキュメント

- **コマンド集**: `docs/CLAUDE_CODE_COMMANDS.md`
- **3ステップガイド**: `docs/QUICK_START_3STEPS.md`
- **哲学**: `.claude/MIYABI_PARALLEL_ORCHESTRA.md`
- **技術詳細**: `.claude/TMUX_OPERATIONS.md`
- **統合インデックス**: `.claude/TMUX_INTEGRATION_INDEX.md`

---

## 🎭 次のステップ

1. **セッションに接続**: `tmux attach -t miyabi-orchestra`
2. **クイックテスト**: 上記の「全Agentに準備確認」コマンドを実行
3. **実践タスク**: 実際のIssue処理を開始

---

**🌸 雅なる並列実行を、お楽しみください！**
