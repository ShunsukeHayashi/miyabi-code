# Database Configuration
# Replace with your actual PostgreSQL connection string
DATABASE_URL="postgresql://username:password@localhost:5432/miyabi_courses?schema=public"

# Alternative format for database connection
# DATABASE_URL="postgresql://miyabi_user:secure_password@localhost:5432/miyabi_courses"

# For Docker development
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/miyabi_courses"

# Production example (use environment-specific credentials)
# DATABASE_URL="postgresql://prod_user:prod_password@db.example.com:5432/miyabi_courses_prod"

# Optional: Individual connection parameters (for docker-compose)
POSTGRES_USER=miyabi_user
POSTGRES_PASSWORD=secure_password
POSTGRES_DB=miyabi_courses
POSTGRES_HOST=localhost
POSTGRES_PORT=5432