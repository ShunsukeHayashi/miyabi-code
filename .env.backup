# ==============================================================================
# Miyabi - Environment Configuration (Termux Edition)
# ==============================================================================
# Generated: 2025-10-15
# Platform: Termux (Android)
# ==============================================================================

# -----------------------------------------------------------------------------
# Required: API Keys
# -----------------------------------------------------------------------------

# GitHub Personal Access Token
# ⚠️ RECOMMENDED: Use 'gh auth login' instead of storing token in .env
# The application will automatically use 'gh auth token' if available
#
# For Termux: Uncomment and add your token if gh CLI is not available
# Scopes required: repo, workflow, write:packages, issues:write
# Generate at: https://github.com/settings/tokens
#
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# Repository Configuration
# -----------------------------------------------------------------------------

# GitHub Repository (owner/repo)
GITHUB_REPOSITORY=ShunsukeHayashi/Miyabi
GITHUB_REPOSITORY_OWNER=ShunsukeHayashi

# Device Identifier (for logs and monitoring)
DEVICE_IDENTIFIER=Termux-Android

# -----------------------------------------------------------------------------
# Rust Configuration
# -----------------------------------------------------------------------------

# Rust logging level
# Options: trace, debug, info, warn, error
RUST_LOG=info

# Rust backtrace
# Options: 0 (disabled), 1 (enabled), full (full backtrace)
RUST_BACKTRACE=1

# -----------------------------------------------------------------------------
# Agent Configuration
# -----------------------------------------------------------------------------

# Log directory
LOG_DIRECTORY=.ai/logs

# Report directory
REPORT_DIRECTORY=.ai/parallel-reports

# Default concurrency for parallel execution
# Termux: Use lower value due to mobile constraints
DEFAULT_CONCURRENCY=1

# -----------------------------------------------------------------------------
# Worktree Configuration
# -----------------------------------------------------------------------------

# Enable Worktree-based parallel execution
USE_WORKTREE=true

# Worktree base directory
WORKTREE_BASE_DIR=.worktrees

# ==============================================================================
# Termux Setup Guide:
#
# 1. Install gh CLI (if not already installed):
#    pkg install gh
#
# 2. Authenticate with GitHub:
#    gh auth login
#
# 3. Verify authentication:
#    gh auth status
#
# 4. Build and run Miyabi:
#    cargo build --release
#    ./target/release/miyabi status
#
# ==============================================================================
