#!/bin/bash
# Miyabi Orchestra クイックスタートコマンド集
# セッション接続後、このファイルの内容をコピペして使用できます

echo "🎭 Miyabi Orchestra - クイックスタートコマンド"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📋 現在のPane構成:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
tmux list-panes -F "Pane #{pane_index}: #{pane_id} - #{pane_width}x#{pane_height}"
echo ""

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🚀 クイックテストコマンド（以下をコピペ）:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

cat << 'EOF'
# ═══════════════════════════════════════════════════
# テスト1: 全Agentの準備確認（これを実行）
# ═══════════════════════════════════════════════════

# カエデ (Pane 1 - %3)
tmux send-keys -t %3 "echo '[カエデ] CodeGenAgent準備完了！'" Enter

# サクラ (Pane 2 - %1)
tmux send-keys -t %1 "echo '[サクラ] ReviewAgent準備完了！'" Enter

# ツバキ (Pane 3 - %4)
tmux send-keys -t %4 "echo '[ツバキ] PRAgent準備完了！'" Enter

# ボタン (Pane 4 - %2)
tmux send-keys -t %2 "echo '[ボタン] DeploymentAgent準備完了！'" Enter


# ═══════════════════════════════════════════════════
# テスト2: Agent動作確認（応答テスト）
# ═══════════════════════════════════════════════════

# カエデに簡単なタスク
tmux send-keys -t %3 "pwd && ls -la | head -5" Enter

# サクラに簡単なタスク
tmux send-keys -t %1 "git status" Enter


# ═══════════════════════════════════════════════════
# テスト3: Pane間の出力確認
# ═══════════════════════════════════════════════════

# 全Paneの最新5行を確認
for pane in %0 %3 %1 %4 %2; do
  echo "=== Pane $pane ==="
  tmux capture-pane -t $pane -p | tail -5
  echo ""
done


# ═══════════════════════════════════════════════════
# Pane操作（Ctrl-bがプレフィックス）
# ═══════════════════════════════════════════════════

# Ctrl-b + 矢印キー  : Pane移動
# Ctrl-b + q         : Pane番号表示（数字キーで移動）
# Ctrl-b + z         : 現在のPaneをフルスクリーン切替
# Ctrl-b + x         : 現在のPaneを削除
# Ctrl-b + d         : セッションからデタッチ（終了せず離脱）


# ═══════════════════════════════════════════════════
# 実践タスク例: 簡単なファイル確認
# ═══════════════════════════════════════════════════

# カエデにREADME確認を依頼
tmux send-keys -t %3 "cat README.md | head -20" Enter

# サクラにCargo.tomlチェックを依頼
tmux send-keys -t %1 "cat Cargo.toml" Enter

# ツバキにGitステータス確認を依頼
tmux send-keys -t %4 "git log --oneline -5" Enter

EOF

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "💡 使い方:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "1. 上記のコマンドブロックをコピー"
echo "2. Conductor pane（メインpane）に貼り付け"
echo "3. 各Paneの反応を観察"
echo ""
echo "📚 詳細ドキュメント:"
echo "  - コマンド集: docs/CLAUDE_CODE_COMMANDS.md"
echo "  - 3ステップガイド: docs/QUICK_START_3STEPS.md"
echo "  - 哲学: .claude/MIYABI_PARALLEL_ORCHESTRA.md"
echo ""
