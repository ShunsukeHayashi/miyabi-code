# チュートリアル: 並列実行の実践

**🎯 このチュートリアルのゴール**: Worktreeを使った並列実行で、複数のIssueを同時に高速処理する

**対象**: チュートリアル「Agent基本操作」を修了した人

**所要時間**: 約30分

---

## 📚 この章で学ぶこと

1. ✅ Worktreeとは何か？
2. ✅ 並列実行のメリット・デメリット
3. ✅ 並列実行の実践手順
4. ✅ トラブルシューティング

---

## Worktree とは？

### 基本概念

**Worktree** は、1つのGitリポジトリで複数のブランチを同時にチェックアウトできる機能です。

**例えるなら**:
- **通常のGit**: 1つの部屋に1人しか入れない
- **Worktree**: 同じ家に複数の部屋があって、各部屋で別々の作業ができる

---

### ディレクトリ構造

```
my-project/                    ← メインのリポジトリ（main ブランチ）
├── src/
├── tests/
└── .worktrees/                ← Worktreeディレクトリ
    ├── issue-100/             ← Issue #100専用の作業スペース
    │   ├── src/
    │   └── tests/
    ├── issue-101/             ← Issue #101専用の作業スペース
    │   ├── src/
    │   └── tests/
    └── issue-102/             ← Issue #102専用の作業スペース
        ├── src/
        └── tests/
```

**ポイント**:
- 各Worktreeは独立したブランチ
- 同時に複数のIssueを処理可能
- コンフリクトの心配なし

---

## 並列実行のメリット

### ✅ メリット

1. **処理時間の短縮**
   - 3つのIssueを並列実行 → 所要時間が3分の1に

2. **コンフリクトの最小化**
   - 各Issueが独立したディレクトリで作業
   - ファイル競合の心配なし

3. **簡単なロールバック**
   - Worktree単位で破棄可能
   - 失敗したらそのWorktreeだけ削除

4. **デバッグが容易**
   - 各Worktreeで独立したログ
   - 問題のあるIssueだけを調査

---

### ⚠️ デメリット

1. **ディスク容量の使用**
   - Worktree 1つあたり、リポジトリ全体のコピーが必要
   - 3つのWorktree → 約3倍のディスク使用量

2. **管理の複雑さ**
   - Worktreeの削除を忘れると残り続ける
   - 定期的なクリーンアップが必要

3. **リソース消費**
   - 並列実行するとCPU・メモリを多く使う
   - マシンスペックに応じた並列数の調整が必要

---

## 並列実行の実践

### ステップ 1: 複数のIssueを作成

まず、並列実行するためのIssueを3つ作成します。

```bash
# Issue #100: Hello World API
gh issue create \
  --title "Add Hello World API endpoint" \
  --body "GET /api/hello を追加" \
  --label "enhancement,📥 state:pending"

# Issue #101: User API
gh issue create \
  --title "Add User API endpoint" \
  --body "GET /api/user/:id を追加" \
  --label "enhancement,📥 state:pending"

# Issue #102: Health Check API
gh issue create \
  --title "Add Health Check endpoint" \
  --body "GET /health を追加" \
  --label "enhancement,📥 state:pending"
```

**期待される出力**:
```
Created issue #100
Created issue #101
Created issue #102
```

---

### ステップ 2: 並列実行の開始

```bash
# Issue #100, #101, #102 を並列実行（並列数: 3）
miyabi parallel --issues 100,101,102 --concurrency 3
```

**期待される出力**:

```
🚀 Miyabi 並列実行モード

処理対象:
  Issue #100: Add Hello World API endpoint
  Issue #101: Add User API endpoint
  Issue #102: Add Health Check endpoint

並列数: 3

📁 Worktreeを作成中...
  ✅ .worktrees/issue-100/ 作成完了
  ✅ .worktrees/issue-101/ 作成完了
  ✅ .worktrees/issue-102/ 作成完了

🔴 CoordinatorAgent を並列起動中...

┌─────────────────────────────────────────┐
│ Worktree #1: Issue #100                 │
│ 🔴 しきるん 起動中...                   │
│   📖 Issue #100 を分析中                │
│   ✂️ 3つのタスクに分解                  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Worktree #2: Issue #101                 │
│ 🔴 しきるん 起動中...                   │
│   📖 Issue #101 を分析中                │
│   ✂️ 4つのタスクに分解                  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Worktree #3: Issue #102                 │
│ 🔴 しきるん 起動中...                   │
│   📖 Issue #102 を分析中                │
│   ✂️ 2つのタスクに分解                  │
└─────────────────────────────────────────┘

⏱️  推定時間: 5分

🟢 CodeGenAgent を並列起動中...
🔵 ReviewAgent を並列起動中...
🟡 PRAgent を並列起動中...

✅ 並列実行完了！

結果:
  ✅ Issue #100 → PR #103 作成
  ✅ Issue #101 → PR #104 作成
  ✅ Issue #102 → PR #105 作成

次のステップ:
gh pr list
```

---

### ステップ 3: 実行中の確認

別のターミナルで、Worktreeの状態を確認できます。

```bash
# Worktree一覧を表示
git worktree list
```

**期待される出力**:

```
/Users/you/my-project              abc1234 [main]
/Users/you/my-project/.worktrees/issue-100  def5678 [feature/issue-100]
/Users/you/my-project/.worktrees/issue-101  ghi9012 [feature/issue-101]
/Users/you/my-project/.worktrees/issue-102  jkl3456 [feature/issue-102]
```

---

### ステップ 4: 各Worktreeの進捗確認

```bash
# Worktree #1 の状態確認
cd .worktrees/issue-100
git status

# ログ確認
cat .ai/logs/$(date +%Y-%m-%d).md
```

---

### ステップ 5: PRを確認

```bash
# 作成されたPR一覧
gh pr list
```

**期待される出力**:

```
#103  ✨ feat: Add Hello World API endpoint  feature/issue-100
#104  ✨ feat: Add User API endpoint         feature/issue-101
#105  ✨ feat: Add Health Check endpoint     feature/issue-102
```

---

### ステップ 6: PRをマージ

```bash
# 全てのPRを一括マージ
gh pr merge 103 --squash --delete-branch
gh pr merge 104 --squash --delete-branch
gh pr merge 105 --squash --delete-branch
```

---

### ステップ 7: Worktreeのクリーンアップ

```bash
# 使用済みWorktreeを削除
miyabi worktree cleanup
```

**期待される出力**:

```
🧹 Worktreeクリーンアップ中...

  ✅ .worktrees/issue-100/ 削除完了
  ✅ .worktrees/issue-101/ 削除完了
  ✅ .worktrees/issue-102/ 削除完了

✅ クリーンアップ完了！
```

---

## 並列数の調整

### 低スペックマシン（4GB RAM、2コアCPU）

```bash
# 並列数: 1（順次実行）
miyabi parallel --issues 100,101,102 --concurrency 1
```

---

### 標準マシン（8GB RAM、4コアCPU）

```bash
# 並列数: 2
miyabi parallel --issues 100,101,102 --concurrency 2
```

---

### 高スペックマシン（16GB RAM、8コアCPU）

```bash
# 並列数: 4〜5
miyabi parallel --issues 100,101,102,103,104 --concurrency 5
```

---

## 実行時間の比較

### 順次実行（concurrency=1）

```
Issue #100: 5分
Issue #101: 7分
Issue #102: 3分
─────────────
合計: 15分
```

---

### 並列実行（concurrency=3）

```
Issue #100: 5分 ┐
Issue #101: 7分 ├─ 並列実行
Issue #102: 3分 ┘
─────────────
合計: 7分（最も遅いIssueの時間）
```

**⏱️ 時間短縮: 約53%**

---

## トラブルシューティング

### Q1. Worktreeが残ったまま

**原因**: クリーンアップを忘れた

**解決方法**:

```bash
# すべてのWorktreeを確認
git worktree list

# 不要なWorktreeを削除
git worktree remove .worktrees/issue-100

# すべてのstaleなWorktreeをクリーンアップ
git worktree prune
```

---

### Q2. ディスク容量不足

**原因**: Worktreeが多すぎる

**解決方法**:

```bash
# Worktree使用量を確認
du -sh .worktrees/*

# 古いWorktreeを削除
miyabi worktree cleanup --all
```

---

### Q3. 並列実行が遅い

**原因**: 並列数が多すぎる

**解決方法**:

```bash
# 並列数を減らす
miyabi parallel --issues 100,101,102 --concurrency 1
```

---

### Q4. コンフリクトが発生

**原因**: mainブランチが更新された

**解決方法**:

```bash
# mainブランチを最新に
git checkout main
git pull origin main

# Worktreeを再作成
miyabi worktree cleanup --all
miyabi parallel --issues 100,101,102 --concurrency 3
```

---

## 高度な使い方

### パターン1: 優先度付き並列実行

```bash
# 高優先度のIssueを先に処理
miyabi parallel \
  --issues 100,101,102 \
  --concurrency 2 \
  --priority high
```

---

### パターン2: Dry Run（テスト実行）

```bash
# 実際には実行せず、計画のみ表示
miyabi parallel \
  --issues 100,101,102 \
  --concurrency 3 \
  --dry-run
```

**期待される出力**:

```
🧪 Dry Run モード

実行計画:
  Worktree #1: Issue #100
    ├─ Task 1: API実装（15分）
    ├─ Task 2: テスト作成（10分）
    └─ Task 3: レビュー（5分）

  Worktree #2: Issue #101
    ├─ Task 1: API実装（20分）
    └─ Task 2: テスト作成（10分）

  Worktree #3: Issue #102
    └─ Task 1: Health Check実装（5分）

推定合計時間: 20分（並列実行）

✅ Dry Run 完了（実際の実行はされていません）
```

---

### パターン3: ウォッチモード

```bash
# リアルタイムで進捗を監視
miyabi parallel \
  --issues 100,101,102 \
  --concurrency 3 \
  --watch
```

**期待される出力**:

```
🔄 ウォッチモード（自動更新: 3秒ごと）

進捗状況:
  Issue #100: [████████░░] 80% (Task 3/3)
  Issue #101: [██████░░░░] 60% (Task 2/3)
  Issue #102: [██████████] 100% ✅

推定残り時間: 2分
```

---

## ベストプラクティス

### ✅ 推奨

1. **並列数は CPU コア数以下**
   ```bash
   # CPU 4コア → 並列数 2〜3
   miyabi parallel --issues ... --concurrency 3
   ```

2. **定期的なクリーンアップ**
   ```bash
   # 毎日クリーンアップ
   miyabi worktree cleanup --older-than 1d
   ```

3. **小さいIssueから並列実行**
   ```bash
   # 規模-小 ラベルのIssueを並列実行
   miyabi parallel --label "規模-小" --concurrency 5
   ```

---

### ❌ 避けるべき

1. **並列数が多すぎる**
   ```bash
   # ❌ NG: 並列数が多すぎてメモリ不足
   miyabi parallel --issues ... --concurrency 20
   ```

2. **依存関係のあるIssueを並列実行**
   ```bash
   # ❌ NG: Issue #101 は Issue #100 に依存
   miyabi parallel --issues 100,101 --concurrency 2
   ```

3. **クリーンアップを忘れる**
   ```bash
   # ❌ NG: Worktreeが溜まる
   # 定期的にクリーンアップすること
   ```

---

## 次のステップ

### このチュートリアルで学んだこと ✅

- ✅ Worktreeの基本概念
- ✅ 並列実行のメリット・デメリット
- ✅ 並列実行の実践手順
- ✅ トラブルシューティング

### 次に学ぶこと

- [ ] **Label System の理解** - 53ラベル体系の活用
- [ ] **カスタムAgent作成** - 独自のAgentを実装
- [ ] **GitHub OS Integration** - GitHub を OS として活用

---

## 📚 参考ドキュメント

- **[Worktree Protocol](../WORKTREE_PROTOCOL.md)** - Worktree完全仕様
- **[Agent運用マニュアル](../AGENT_OPERATIONS_MANUAL.md)** - Agent運用の詳細

---

**🚀 次のドキュメント**: [Label System初心者ガイド](../tutorials/04-label-basics.md)

**⬅ 前のページ**: [Agent基本操作](02-agent-basics.md)

---

🤖 Generated with Claude Code
