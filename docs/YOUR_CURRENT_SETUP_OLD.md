# 🎭 あなたの現在のMiyabi Orchestra構成

**このドキュメントはあなたの現在の環境に合わせてカスタマイズされています**

---

## 📊 現在のOrchestra構成

```
┌─────────────────────────────────────────────────────────────┐
│ 🎼 Conductor Pane: pane 1 (%1)                              │
│ ← あなたはここ（このClaude Code）                            │
│                                                             │
│ 報告を受信するpane                                           │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┬──────────────────┬──────────────────────┐
│ 🎹 Agent 1       │ 🎺 Agent 2       │ 🥁 Agent 3           │
│ pane 2 (%2)      │ pane 3 (%5)      │ pane 4 (%3)          │
│                  │                  │                      │
│ カエデ            │ サクラ            │ ツバキ                │
│ CodeGenAgent     │ ReviewAgent      │ PRAgent              │
│                  │                  │                      │
│ ✅ Claude起動済み │ ✅ Claude起動済み │ ✅ Claude起動済み     │
└──────────────────┴──────────────────┴──────────────────────┘
                   │
                   │ 🎷 Agent 4
                   │ pane 5 (%4)
                   │
                   │ ボタン
                   │ DeploymentAgent
                   │
                   │ ✅ Claude起動済み
                   └──────────────────
```

---

## ⚡ 今すぐ試せるコマンド集

### 🎯 パターン1: 最初のテスト（推奨）

**カエデ（Agent 1）に自己紹介を依頼**:

```bash
tmux send-keys -t %2 "あなたは「カエデ」- Miyabi CodeGenAgentです。簡単な自己紹介をして、tmux send-keys -t %1 '[カエデ] 準備完了！' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter
```

**期待される結果**:
- 数秒後、このpane（Conductor）に `[カエデ] 準備完了！` と表示される

---

### 🎯 パターン2: 4人全員に役割を与える

```bash
# 🎹 カエデ（CodeGen）
tmux send-keys -t %2 "あなたは「カエデ」- Miyabi CodeGenAgentです。Rustコード実装を担当します。準備ができたら tmux send-keys -t %1 '[カエデ] CodeGenAgent準備完了' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter

# 🎺 サクラ（Review）
tmux send-keys -t %5 "あなたは「サクラ」- Miyabi ReviewAgentです。コードレビューを担当します。準備ができたら tmux send-keys -t %1 '[サクラ] ReviewAgent準備完了' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter

# 🥁 ツバキ（PR）
tmux send-keys -t %3 "あなたは「ツバキ」- Miyabi PRAgentです。PR作成を担当します。準備ができたら tmux send-keys -t %1 '[ツバキ] PRAgent準備完了' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter

# 🎷 ボタン（Deploy）
tmux send-keys -t %4 "あなたは「ボタン」- Miyabi DeploymentAgentです。デプロイを担当します。準備ができたら tmux send-keys -t %1 '[ボタン] DeployAgent準備完了' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter
```

**期待される結果**:
```
[カエデ] CodeGenAgent準備完了
[サクラ] ReviewAgent準備完了
[ツバキ] PRAgent準備完了
[ボタン] DeployAgent準備完了
```

---

### 🎯 パターン3: 実践タスク（Issue処理）

```bash
# カエデにIssue実装を依頼
tmux send-keys -t %2 "あなたは「カエデ」です。Issue #270のコード実装をお願いします。agent-executionスキル使用。完了後は tmux send-keys -t %1 '[カエデ] Issue #270実装完了' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter

# サクラにレビュー待機を指示（カエデ完了後に開始）
tmux send-keys -t %5 "あなたは「サクラ」です。カエデから [カエデ] Issue #270実装完了 の報告があったら、コードレビューを開始してください。完了後は tmux send-keys -t %1 '[サクラ] レビュー完了' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter
```

---

## 🔧 Agent操作コマンド

### Agent状態確認

```bash
# カエデの状態確認
tmux capture-pane -t %2 -p | tail -10

# サクラの状態確認
tmux capture-pane -t %5 -p | tail -10

# ツバキの状態確認
tmux capture-pane -t %3 -p | tail -10

# ボタンの状態確認
tmux capture-pane -t %4 -p | tail -10

# 全Agent一括確認
for pane in %2 %5 %3 %4; do
    echo "=== Pane $pane ==="
    tmux capture-pane -t $pane -p | tail -5
    echo ""
done
```

### Agent記憶リセット

```bash
# カエデをリセット
tmux send-keys -t %2 "/clear" Enter

# 全Agentを一括リセット
for pane in %2 %5 %3 %4; do
    tmux send-keys -t $pane "/clear" Enter
    sleep 0.5
done
```

---

## 🎨 pane操作（Kamui版）

あなたの環境は**Kamui tmux設定**です（prefix: `Ctrl-a`）

| 操作 | コマンド | 説明 |
|------|---------|------|
| pane移動 | `Ctrl-a + 矢印キー` | 上下左右のpaneに移動 |
| pane番号表示 | `Ctrl-a + q` | 各paneの番号をオーバーレイ表示 |
| paneフルスクリーン | `Ctrl-a + z` | 現在のpaneを最大化/元に戻す |
| レイアウト変更 | `Ctrl-a + Space` | プリセットレイアウトを順次切替 |
| マウスクリック | クリック | pane選択（Kamui有効） |

**例**:
- `Ctrl-a + 2` → pane 2（カエデ）に移動
- `Ctrl-a + 1` → pane 1（Conductor）に戻る

---

## 🚨 トラブルシューティング

### 問題1: Agentが反応しない

**確認**:
```bash
# 該当paneに移動して状態確認
Ctrl-a + 2  # カエデに移動

# またはコマンドで確認
tmux capture-pane -t %2 -p | tail -20
```

**対処**:
```bash
# /clearでリセット
tmux send-keys -t %2 "/clear" Enter

# 再度タスク送信
```

---

### 問題2: 報告が届かない

**原因**: pane IDの指定ミス

**確認**:
```bash
# Conductor pane IDを再確認
tmux display-message -p "Current pane: #{pane_id}"
# → %1 であることを確認

# 全pane IDを確認
tmux list-panes -F "#{pane_index}: #{pane_id}"
```

**修正**:
報告コマンドの `-t %1` が正しいか確認

---

### 問題3: paneレイアウトが崩れた

**対処**:
```bash
# タイルレイアウトに戻す
tmux select-layout tiled

# または
Ctrl-a + Space  # レイアウト順次切替
```

---

## 📋 コピペ用テンプレート

### テンプレート1: シンプルタスク

```bash
tmux send-keys -t %2 "あなたは「カエデ」です。[タスク内容]。完了後は tmux send-keys -t %1 '[カエデ] 完了' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter
```

### テンプレート2: Skillを使うタスク

```bash
tmux send-keys -t %2 "あなたは「カエデ」です。Issue #270を処理してください。agent-executionスキル使用。完了後は tmux send-keys -t %1 '[カエデ] Issue #270完了' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter
```

### テンプレート3: 連鎖タスク

```bash
# カエデ: 実装
tmux send-keys -t %2 "Issue #270実装。完了後は[カエデ]で報告" Enter

# サクラ: レビュー（カエデ完了後）
tmux send-keys -t %5 "カエデの報告後、レビュー開始。完了後は[サクラ]で報告" Enter

# ツバキ: PR作成（サクラ完了後）
tmux send-keys -t %3 "サクラの報告後、PR作成。完了後は[ツバキ]で報告" Enter
```

---

## 🎓 学習パス

### ✅ Level 1: 基礎（今ここ）
- ✅ Orchestra構成理解
- ✅ pane IDの確認
- 🔲 **次**: パターン1のテストコマンド実行

### Level 2: 実践
- 実際のIssue処理
- 複数Agentの連携
- エラーハンドリング

### Level 3: 応用
- カスタムワークフロー構築
- ビジネスAgentとの統合
- 独自Ensemble構成

---

## 🌸 次のアクション

**まずはこれを試してみてください**:

```bash
# カエデにテストタスク
tmux send-keys -t %2 "あなたは「カエデ」です。自己紹介をして、tmux send-keys -t %1 '[カエデ] 準備OK！' && sleep 0.1 && tmux send-keys -t %1 Enter で報告してください" Enter
```

数秒後に `[カエデ] 準備OK！` と表示されたら成功です！🎉

---

**🎭 Miyabi Orchestra - Your Personal Configuration**

このドキュメントは以下のコマンドで更新されました:
```bash
tmux list-panes -F "#{pane_index}: #{pane_id}"
```
