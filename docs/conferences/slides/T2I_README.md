# T2I Image Generation for Miyabi Presentation

このディレクトリには、AI駆動開発カンファレンス 2025秋のプレゼンテーション資料用に、Text-to-Image (T2I) 技術を使って画像を自動生成するスクリプトが含まれています。

## 📋 ファイル一覧

- `generate-images.js` - BytePlus ARK APIを使った画像生成スクリプト
- `update-html.js` - 生成された画像をHTMLに統合するスクリプト
- `index.html` - プレゼンテーションHTMLファイル（T2Iプレースホルダー含む）
- `styles.css` - スタイルシート
- `script.js` - インタラクティブ機能
- `images/` - 生成された画像の保存ディレクトリ（自動作成）

---

## 🚀 クイックスタート

### ステップ1: API キーの設定

BytePlus ARK APIキーを環境変数に設定：

```bash
export BYTEPLUS_API_KEY=your_api_key_here
```

**API キーの取得方法**:
1. BytePlus ARK ポータルにアクセス: https://console.volcengine.com/ark
2. アカウントを作成またはログイン
3. API キーセクションで新しいキーを生成
4. キーをコピーして環境変数に設定

### ステップ2: 依存関係の確認

このスクリプトはNode.js標準ライブラリのみを使用しているため、追加のnpmインストールは不要です。

必要なもの:
- Node.js v14以上

確認:
```bash
node --version
```

### ステップ3: 画像生成の実行

全9枚の画像を生成：

```bash
node generate-images.js
```

**実行時間**: 約20-30秒（2秒のレート制限間隔含む）

### ステップ4: HTMLの更新

生成された画像をHTMLに統合：

```bash
node update-html.js
```

### ステップ5: プレゼンテーションの確認

ブラウザでHTMLを開く：

```bash
# macOS
open index.html

# Linux
xdg-open index.html

# Windows
start index.html
```

---

## 🎨 生成される画像

### 1. agent-icons-background.png
- **用途**: スライド1 - タイトル背景
- **サイズ**: 1920x1080
- **内容**: 21個のAIロボットアイコン、ホログラフィックスタイル

### 2. profile-professional.png
- **用途**: スライド2 - プロフィール写真
- **サイズ**: 400x400
- **内容**: プロフェッショナルなポートレート写真

### 3. github-contributions.png
- **用途**: スライド2 - GitHub活動グラフ
- **サイズ**: 600x200
- **内容**: GitHubコントリビューションヒートマップ

### 4. tool-comparison-arrow.png
- **用途**: スライド4 - ツール比較
- **サイズ**: 800x300
- **内容**: Copilot/Cursor ロゴと「ここまで」矢印

### 5. ai-levels-pyramid.png
- **用途**: スライド6 - AI駆動開発の3レベル
- **サイズ**: 1200x800
- **内容**: 3段階ピラミッド図（Level 1-3）

### 6. github-os-architecture.png
- **用途**: スライド10 - GitHub as OS アーキテクチャ
- **サイズ**: 1400x800
- **内容**: GitHub OSアーキテクチャ図（Issues, Projects, Webhooks, Actions, Labels）

### 7. coding-agents-flowchart.png
- **用途**: スライド12 - Coding Agents フロー
- **サイズ**: 1600x400
- **内容**: 5つのAgent（Coordinator → CodeGen → Review → PR → Deployment）

### 8. rust-performance-comparison.png
- **用途**: スライド17 - Rust実装のメリット
- **サイズ**: 1200x600
- **内容**: Before/After 比較チャート（実行速度50%向上、メモリ30%削減）

### 9. github-qr-code.png
- **用途**: スライド40 - Q&A
- **サイズ**: 400x400
- **内容**: GitHub リポジトリへのQRコード

---

## 🔧 カスタマイズ

### プロンプトの編集

`generate-images.js` の `IMAGE_PROMPTS` 配列を編集して、プロンプトをカスタマイズできます：

```javascript
{
    id: 'custom-image',
    filename: 'custom-image.png',
    prompt: 'Your custom prompt here',
    width: 1200,
    height: 800
}
```

### 画像サイズの変更

各画像の `width` と `height` プロパティを編集：

```javascript
{
    id: 'agent-icons-bg',
    filename: 'agent-icons-background.png',
    prompt: '...',
    width: 2560,  // 4K対応
    height: 1440
}
```

### モデルの変更

別のT2Iモデルを使用したい場合は、`generate-images.js` の `MODEL` 定数を編集：

```javascript
const MODEL = 'your-preferred-model';
```

---

## 🔍 トラブルシューティング

### エラー: API Key not set

**症状**:
```
❌ Error: BYTEPLUS_API_KEY environment variable not set
```

**解決策**:
```bash
export BYTEPLUS_API_KEY=your_actual_api_key
node generate-images.js
```

### エラー: API Error

**症状**:
```
❌ Failed: API Error: Invalid request
```

**原因**:
- APIキーが無効
- APIクォータ超過
- ネットワーク問題

**解決策**:
1. APIキーを確認
2. BytePlus ARK ポータルでクォータを確認
3. ネットワーク接続を確認
4. しばらく待ってから再試行

### エラー: Missing generated images

**症状**:
```
❌ Error: Missing generated images:
   - agent-icons-background.png
```

**解決策**:
```bash
# 画像生成スクリプトを先に実行
node generate-images.js

# 生成完了後にHTML更新
node update-html.js
```

### 画像の品質が低い

**解決策**:
1. プロンプトをより詳細に記述
2. 画像サイズを大きくする（例: 2560x1440）
3. 異なるモデルを試す
4. プロンプトに品質キーワードを追加（"high quality", "professional", "4K"）

### HTMLが更新されない

**原因**: パターンマッチングの失敗

**解決策**:
1. `index.html.backup` から復元:
   ```bash
   cp index.html.backup index.html
   ```
2. `update-html.js` の `searchPattern` を確認
3. 再実行:
   ```bash
   node update-html.js
   ```

---

## 📊 パフォーマンス

### API レート制限

BytePlus ARK API は、リクエスト間に最低2秒の間隔を推奨しています。

現在の設定:
- **間隔**: 2秒
- **合計画像数**: 9枚
- **推定時間**: 約20-30秒

レート制限を調整するには、`generate-images.js` の待機時間を変更：

```javascript
await new Promise(resolve => setTimeout(resolve, 3000)); // 3秒に変更
```

### API コスト

BytePlus ARK API の料金:
- **モデル**: seedream-4-0-250828
- **料金**: リクエストごとに課金（詳細は公式ドキュメント参照）

**推定コスト** (9枚):
- 低解像度画像: $0.XX - $X.XX
- 高解像度画像: $X.XX - $XX.XX

---

## 🎯 ベストプラクティス

### プロンプト作成のコツ

1. **具体的に記述**
   - ❌ "diagram"
   - ✅ "flowchart showing 5 connected stages with icons and arrows"

2. **スタイルを指定**
   - "modern flat design"
   - "professional presentation style"
   - "minimalist aesthetic"

3. **色を指定**
   - "blue and purple gradient"
   - "white background"
   - "dark theme with green accents"

4. **品質キーワード**
   - "high quality"
   - "photorealistic"
   - "clean and professional"

### 画像の最適化

生成後、画像を最適化してファイルサイズを削減：

```bash
# ImageMagickを使用（インストール: brew install imagemagick）
mogrify -strip -quality 85 images/*.png

# TinyPNGを使用（オンライン: https://tinypng.com）
```

### バックアップ

重要な画像は必ずバックアップ：

```bash
# imagesディレクトリをバックアップ
cp -r images images-backup

# HTMLをバックアップ（自動）
# update-html.js が index.html.backup を自動作成
```

---

## 🔄 ワークフロー

### 初回セットアップ

```bash
# 1. API キー設定
export BYTEPLUS_API_KEY=your_api_key

# 2. 画像生成
node generate-images.js

# 3. HTML更新
node update-html.js

# 4. プレビュー
open index.html
```

### 画像の再生成

特定の画像を再生成したい場合:

1. `generate-images.js` の `IMAGE_PROMPTS` から該当画像のみを残す
2. スクリプトを実行
3. 生成された画像を `images/` ディレクトリに配置
4. `update-html.js` を実行

### プレゼンテーション配布

```bash
# 1. すべてのファイルをまとめる
mkdir miyabi-presentation
cp index.html styles.css script.js miyabi-presentation/
cp -r images miyabi-presentation/

# 2. ZIPファイル作成
zip -r miyabi-presentation.zip miyabi-presentation/

# 3. 配布
# - Google Driveにアップロード
# - GitHub Pagesにデプロイ
# - USBメモリにコピー
```

---

## 📚 参考資料

### BytePlus ARK API

- **公式ドキュメント**: https://www.volcengine.com/docs/ark/api-reference
- **モデル一覧**: https://www.volcengine.com/docs/ark/models
- **料金**: https://www.volcengine.com/docs/ark/pricing

### Reveal.js

- **公式サイト**: https://revealjs.com
- **ドキュメント**: https://revealjs.com/docs
- **テーマ**: https://revealjs.com/themes

### AOS (Animate On Scroll)

- **GitHub**: https://github.com/michalsnik/aos
- **デモ**: https://michalsnik.github.io/aos

### Font Awesome

- **公式サイト**: https://fontawesome.com
- **アイコン検索**: https://fontawesome.com/icons

---

## ✅ チェックリスト

完璧なプレゼンテーションのための最終チェック:

- [ ] BytePlus ARK APIキーを設定
- [ ] `node generate-images.js` を実行
- [ ] 全9枚の画像が `images/` ディレクトリに生成された
- [ ] `node update-html.js` を実行
- [ ] HTMLに画像が正しく統合された
- [ ] ブラウザでプレゼンテーションをプレビュー
- [ ] 全スライドの画像が正しく表示される
- [ ] レスポンシブデザインを確認（異なる画面サイズ）
- [ ] アニメーションが正常に動作
- [ ] キーボードショートカット（b, w, f, s, o）が動作
- [ ] 画像の品質を確認（必要に応じて再生成）
- [ ] プレゼンテーションファイルをバックアップ
- [ ] 配布用ZIPファイルを作成

---

## 🎉 次のステップ

1. **リハーサル**: 30分セッションの時間配分を確認
2. **調整**: スライドの内容や画像を微調整
3. **PDF出力**: ブラウザの印刷機能で PDF として保存
4. **アップロード**: Google Drive や GitHub Pages にデプロイ
5. **共有**: 参加者にプレゼンテーション資料を共有

---

**作成日**: 2025-10-22

**作成者**: Claude Code (AI Assistant)

**バージョン**: 1.0.0

**ライセンス**: MIT License
