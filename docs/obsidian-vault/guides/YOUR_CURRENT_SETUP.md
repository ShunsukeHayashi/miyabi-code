---
title: "🎭 あなたの現在のMiyabi Orchestra構成（Claude Code対応版）"
created: 2025-11-18
updated: 2025-11-18
author: "Miyabi Team"
category: "guides"
tags: ['agents', 'ai', 'autonomous', 'claude-code', 'guide', 'guides', 'miyabi', 'multiplexer', 'quick-start', 'terminal', 'tmux']
status: "published"
---

# 🎭 あなたの現在のMiyabi Orchestra構成（Claude Code対応版）

**Claude Codeのインタラクティブモード用に最適化**

---

## 📋 前提条件

⚠️ **このドキュメントは、すでにOrchestraをセットアップ済みの方向けです。**

**初めての方へ**:
1. [QUICK_START_3STEPS.md](./QUICK_START_3STEPS.md) - 3分でセットアップ
2. このドキュメント - 現在の構成確認

---

## 📖 Miyabi Entity-Relation Model

このドキュメントで扱うEntity:
- **E3 (Agent)**: 自律実行Agent（カエデ、サクラ、ツバキ、ボタン）
- **E2 (Task)**: Issueから分解されたタスク
- **E7 (Command)**: Claude Codeコマンド（`tmux send-keys`経由で実行）
- **E12 (Worktree)**: Git Worktree（並列実行環境）

主要Relationship:
- **R9**: Agent executes Task（AgentがTaskを実行）
- **R15**: Command invoked-by Agent（コマンドがAgentを起動）
- **R8**: Task runs-in Worktree（タスクがWorktree内で実行）

詳細: [ENTITY_RELATION_MODEL.md](../docs/architecture/ENTITY_RELATION_MODEL.md)

---

## ⚠️ 重要: 基本スタイル

**Claude Codeのインタラクティブモードでは**:
```bash
tmux send-keys -t %N "cd '/Users/shunsuke/Dev/miyabi-private' && [指示内容]" && sleep 0.1 && tmux send-keys -t %N Enter
```

**重要な要素**:
1. ✅ **ダブルクォート**を使用
2. ✅ **cd コマンド**でワーキングディレクトリに移動
3. ✅ **&& sleep 0.1** を挟む
4. ✅ **その後 Enter を送信**

---

## 📊 現在のOrchestra構成

```
┌─────────────────────────────────────────────────────────────┐
│ 🎼 Conductor Pane: pane 1 (%1)                              │
│ ← あなたはここ（このClaude Code）                            │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┬──────────────────┬──────────────────────┐
│ 🎹 Agent 1       │ 🎺 Agent 2       │ 🥁 Agent 3           │
│ pane 2 (%2)      │ pane 3 (%5)      │ pane 4 (%3)          │
│ カエデ            │ サクラ            │ ツバキ                │
│ ✅ Claude起動済み │ ✅ Claude起動済み │ ✅ Claude起動済み     │
└──────────────────┴──────────────────┴──────────────────────┘
                   │ 🎷 Agent 4
                   │ pane 5 (%4)
                   │ ボタン
                   │ ✅ Claude起動済み
                   └──────────────────
```

---

## ⚡ 今すぐ試せるコマンド（Claude Code対応）

### 🎯 パターン1: 最初のテスト（推奨）⭐

```bash
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && あなたは「カエデ」です。準備ができたら [カエデ] 準備OK と発言してください。" && sleep 0.1 && tmux send-keys -t %2 Enter
```

**確認方法**:
1. `Ctrl-a + 2` でpane 2（カエデ）に移動
2. カエデの発言を確認
3. `Ctrl-a + 1` で戻る

---

### 🎯 パターン2: 4人全員に役割を与える

```bash
# 🎹 カエデ
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && あなたは「カエデ」- Miyabi CodeGenAgentです。Rustコード実装担当。準備完了したら [カエデ] 準備完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %2 Enter

# 🎺 サクラ
tmux send-keys -t %5 "cd '/Users/shunsuke/Dev/miyabi-private' && あなたは「サクラ」- Miyabi ReviewAgentです。コードレビュー担当。準備完了したら [サクラ] 準備完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %5 Enter

# 🥁 ツバキ
tmux send-keys -t %3 "cd '/Users/shunsuke/Dev/miyabi-private' && あなたは「ツバキ」- Miyabi PRAgentです。PR作成担当。準備完了したら [ツバキ] 準備完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %3 Enter

# 🎷 ボタン
tmux send-keys -t %4 "cd '/Users/shunsuke/Dev/miyabi-private' && あなたは「ボタン」- Miyabi DeploymentAgentです。デプロイ担当。準備完了したら [ボタン] 準備完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %4 Enter
```

**確認**: 各paneに移動して発言を確認

---

### 🎯 パターン3: 実践タスク（Issue処理）

```bash
# カエデにIssue実装
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && Issue #270のコード実装をagent-executionスキルで行ってください。完了したら [カエデ] Issue #270完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %2 Enter

# サクラにレビュー待機
tmux send-keys -t %5 "cd '/Users/shunsuke/Dev/miyabi-private' && カエデが Issue #270完了 と発言したら、コードレビューを開始してください。完了したら [サクラ] レビュー完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %5 Enter
```

---

## 🔧 Agent操作コマンド

### Agent状態確認

```bash
# カエデの最新10行を表示
tmux capture-pane -t %2 -p | tail -10

# または直接paneに移動
Ctrl-a + 2
```

### 全Agent一括確認

```bash
for pane in %2 %5 %3 %4; do echo "=== $pane ==="; tmux capture-pane -t $pane -p | tail -5; echo ""; done
```

### Agent記憶リセット

```bash
# カエデをリセット
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && /clear" && sleep 0.1 && tmux send-keys -t %2 Enter

# 全Agent一括リセット
for pane in %2 %5 %3 %4; do tmux send-keys -t $pane "cd '/Users/shunsuke/Dev/miyabi-private' && /clear" && sleep 0.1 && tmux send-keys -t $pane Enter; sleep 0.5; done
```

---

## 🎨 pane操作（Kamui版）

| 操作 | コマンド | 説明 |
|------|---------|------|
| pane移動 | `Ctrl-a + 矢印` | 上下左右に移動 |
| pane番号表示 | `Ctrl-a + q` | 番号オーバーレイ |
| pane 2に移動 | `Ctrl-a + 2` | カエデへ |
| pane 1に戻る | `Ctrl-a + 1` | Conductorへ |
| フルスクリーン | `Ctrl-a + z` | 最大化/解除 |

---

## 🚨 トラブルシューティング

### 問題1: Agentが反応しない

**確認**:
```bash
Ctrl-a + 2  # カエデに移動して状態確認
```

**対処**:
```bash
tmux send-keys -t %2 '/clear' Enter  # リセット
```

---

### 問題2: コマンドが文字列として表示される

**原因**: 改行を含むコマンドを送信した

**解決**: このドキュメントのワンライナーコマンドを使用

**例（悪い）**:
```bash
tmux send-keys -t %2 "あなたは
カエデです" && sleep 0.1 && tmux send-keys -t %2 Enter  # ❌ 改行あり
```

**例（良い）**:
```bash
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && あなたはカエデです" && sleep 0.1 && tmux send-keys -t %2 Enter  # ✅ 1行
```

---

### 問題2-2: Enterで改行されてしまう（解決済み）

**原因**: `Enter`を直接使用すると、Claude Codeの入力ボックスで改行される

**解決**: sleep を挟んで Enter を分離
```bash
# ❌ 悪い例
tmux send-keys -t %2 'コマンド' Enter

# ❌ これも動かない
tmux send-keys -t %2 'コマンド' C-m

# ✅ 正解
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && コマンド" && sleep 0.1 && tmux send-keys -t %2 Enter
```

---

### 問題3: Agentの発言が見つからない

**方法1**: paneに移動して会話履歴を確認
```bash
Ctrl-a + 2  # カエデに移動
# スクロールアップして確認
```

**方法2**: コマンドで確認
```bash
tmux capture-pane -t %2 -p | grep "\[カエデ\]"
```

---

## 📋 コピペ用テンプレート

### テンプレート1: シンプルタスク

```bash
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && [タスク内容]。完了したら [カエデ] 完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %2 Enter
```

### テンプレート2: Skillを使うタスク

```bash
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && [タスク内容]。[スキル名]スキルを使用してください。完了したら [カエデ] 完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %2 Enter
```

### テンプレート3: 連鎖タスク

```bash
tmux send-keys -t %5 "cd '/Users/shunsuke/Dev/miyabi-private' && カエデが [完了メッセージ] と発言したら、[タスク内容]を開始してください。" && sleep 0.1 && tmux send-keys -t %5 Enter
```

---

## 💡 実践例: Issue #270を4人で処理

```bash
# Step 1: カエデに実装依頼
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && Issue #270のRust実装をagent-executionスキルで行ってください。完了したら [カエデ] Issue #270実装完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %2 Enter

# Step 2: サクラにレビュー待機
tmux send-keys -t %5 "cd '/Users/shunsuke/Dev/miyabi-private' && カエデが Issue #270実装完了 と発言したら、コードレビューを開始してください。完了したら [サクラ] レビュー完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %5 Enter

# Step 3: ツバキにPR作成待機
tmux send-keys -t %3 "cd '/Users/shunsuke/Dev/miyabi-private' && サクラが レビュー完了 と発言したら、PR作成を開始してください。完了したら [ツバキ] PR作成完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %3 Enter

# Step 4: ボタンにデプロイ待機
tmux send-keys -t %4 "cd '/Users/shunsuke/Dev/miyabi-private' && ツバキが PR作成完了 と発言したら、デプロイを開始してください。完了したら [ボタン] デプロイ完了 と発言してください。" && sleep 0.1 && tmux send-keys -t %4 Enter
```

**監視方法**:
- `Ctrl-a + 2` → カエデの作業確認
- `Ctrl-a + 3` → サクラの作業確認
- `Ctrl-a + 4` → ツバキの作業確認
- `Ctrl-a + 1` → Conductorに戻る

---

## 🌸 次のアクション

**まずはこれを試してみてください**:

```bash
tmux send-keys -t %2 "cd '/Users/shunsuke/Dev/miyabi-private' && あなたは「カエデ」です。準備ができたら [カエデ] 準備OK と発言してください。" && sleep 0.1 && tmux send-keys -t %2 Enter
```

**確認**:
```bash
Ctrl-a + 2  # カエデに移動
# [カエデ] 準備OK が見えたら成功！
Ctrl-a + 1  # Conductorに戻る
```

---

## 📚 関連ドキュメント

- **⚡ Claude Code Commands**: [CLAUDE_CODE_COMMANDS.md](./CLAUDE_CODE_COMMANDS.md) ← 最新版！
- **🌸 Philosophy**: [MIYABI_PARALLEL_ORCHESTRA.md](../.claude/MIYABI_PARALLEL_ORCHESTRA.md)
- **⚡ 3-Step Guide**: [QUICK_START_3STEPS.md](./QUICK_START_3STEPS.md)

---

**🎭 Miyabi Orchestra - Claude Code対応版**

---

## 📚 Related Documents

- [[QUICK_START_3STEPS]]
- [[TMUX_QUICKSTART]]
- [[CLAUDE_CODE_COMMANDS]]
