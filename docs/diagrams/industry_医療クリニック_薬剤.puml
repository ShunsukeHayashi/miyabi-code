@startuml industry_医療クリニック_薬剤
!theme cerulean-outline

title 医療・クリニック - 薬剤部門 業務フロー

|薬剤|
start
:処方箋受領;

:処方内容確認;
note right
  - 患者情報
  - 薬剤名
  - 用法用量
  - 日数
end note

if (疑義あり?) then (はい)
  |診療|
  :処方医へ問い合わせ;
  :回答受領;

  |薬剤|
  if (処方変更?) then (はい)
    :処方修正確認;
  else (いいえ)
    :疑義解消記録;
  endif
else (いいえ)
endif

:患者薬歴確認;

if (相互作用・禁忌?) then (はい)
  |診療|
  :処方医へ報告;

  |薬剤|
  :処方変更or継続確認;
else (いいえ)
endif

if (アレルギー該当?) then (はい)
  |診療|
  :処方医へ報告;

  |薬剤|
  :代替薬提案;
else (いいえ)
endif

:調剤開始;

fork
  :内服薬調剤;
  :一包化(必要時);
fork again
  :外用薬調剤;
fork again
  :注射薬調製;
end fork

:調剤監査;
note right
  - 薬剤照合
  - 数量確認
  - 規格確認
end note

if (監査OK?) then (はい)
  :監査完了記録;
else (いいえ)
  :再調剤;
  :再監査;
endif

:患者呼び出し;

:服薬指導;
note right
  - 薬効説明
  - 用法用量説明
  - 副作用説明
  - 注意事項
  - 食事との関係
  - 保管方法
end note

if (患者理解OK?) then (はい)
  :薬剤交付;
else (いいえ)
  :再説明;
  :理解確認;
endif

:薬歴記録;

:お薬手帳記載;

|受付|
:会計連絡;

|薬剤|
' 在庫管理業務
fork
  :在庫確認;
  if (在庫不足?) then (はい)
    :発注処理;
    :卸業者連絡;
  else (いいえ)
  endif
fork again
  :期限管理;
  if (期限切れ近い?) then (はい)
    :優先使用or廃棄処理;
  else (いいえ)
  endif
fork again
  :麻薬・向精神薬管理;
  :帳簿記載;
end fork

:業務終了;

stop

@enduml
