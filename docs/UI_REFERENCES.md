# Miyabi UI実装 - 参考UI調査レポート

**作成日**: 2025-10-22
**調査対象**: shadcn/ui Dashboard Examples on GitHub

---

## 📋 調査概要

Next.js + shadcn/ui + Tailwind CSSで構築された優れたダッシュボードUIを調査しました。
以下の5つの代表例を参考にMiyabi UIを設計します。

---

## 🌟 参考プロジェクト

### 1. satnaing/shadcn-admin ⭐ 推奨度: ★★★★★

**リポジトリ**: https://github.com/satnaing/shadcn-admin

**特徴**:
- Vite + React + shadcn/ui
- RTL（右→左言語）サポート
- カスタマイズされたコンポーネント
- ダークモード完全対応
- レスポンシブデザイン

**学ぶべきポイント**:
- サイドバーナビゲーションの実装
- カードレイアウトの美しい配置
- アイコンの効果的な使用
- カラースキームの統一感

**Miyabiへの適用**:
```typescript
// サイドバーレイアウト
<div className="flex h-screen">
  <Sidebar /> {/* 固定サイドバー */}
  <main className="flex-1 overflow-y-auto">
    {children}
  </main>
</div>
```

---

### 2. Kiranism/next-shadcn-dashboard-starter ⭐ 推奨度: ★★★★★

**リポジトリ**: https://github.com/Kiranism/next-shadcn-dashboard-starter

**特徴**:
- Next.js 15対応
- Recharts統合（グラフ表示）
- Clerk認証統合
- アナリティクスカード

**学ぶべきポイント**:
- Rechartsを使った美しいグラフ表示
- カード内のメトリクス表示（実行中Agent数等に応用）
- 認証フローの実装パターン

**Miyabiへの適用**:
```typescript
// ダッシュボードサマリーカード
<Card>
  <CardHeader>
    <CardTitle>実行中Agent</CardTitle>
  </CardHeader>
  <CardContent>
    <div className="text-4xl font-bold">{runningCount}</div>
    <p className="text-sm text-muted-foreground">
      +20% from last month
    </p>
  </CardContent>
</Card>
```

---

### 3. shadcn/ui 公式ダッシュボード例 ⭐ 推奨度: ★★★★★

**ライブデモ**: https://ui.shadcn.com/examples/dashboard
**ソースコード**: https://github.com/shadcn-ui/ui/blob/main/apps/www/app/(app)/examples/dashboard/page.tsx

**特徴**:
- 公式の推奨実装パターン
- シンプルで洗練されたデザイン
- コンポーネントの正しい使い方

**学ぶべきポイント**:
- Tabsの使い方
- データテーブルの実装
- Cardsのグリッドレイアウト

**Miyabiへの適用**:
```typescript
// Tabsを使ったAgent切り替え
<Tabs defaultValue="all">
  <TabsList>
    <TabsTrigger value="all">全て</TabsTrigger>
    <TabsTrigger value="coding">Coding</TabsTrigger>
    <TabsTrigger value="business">Business</TabsTrigger>
  </TabsList>
  <TabsContent value="all">
    <AgentList agents={allAgents} />
  </TabsContent>
</Tabs>
```

---

### 4. arhamkhnz/next-shadcn-admin-dashboard ⭐ 推奨度: ★★★★

**リポジトリ**: https://github.com/arhamkhnz/next-shadcn-admin-dashboard

**特徴**:
- Next.js 15対応
- 複数のカラーテーマプリセット
- モダンなデザイン

**学ぶべきポイント**:
- カラーテーマの切り替え機能
- 設定画面のUI

**Miyabiへの適用**:
- ユーザー設定画面でカラーテーマ選択
- ダークモード/ライトモード切り替え

---

### 5. awesome-shadcn-ui（リソース集） ⭐ 推奨度: ★★★★

**リポジトリ**: https://github.com/birobirobiro/awesome-shadcn-ui

**特徴**:
- shadcn/ui関連の厳選リソース集
- コンポーネント、テンプレート、ツール

**学ぶべきポイント**:
- コミュニティのベストプラクティス
- 追加コンポーネントの発見

---

## 🎨 UI設計パターン（Miyabi適用）

### パターン1: ダッシュボードレイアウト

```
┌────────────────────────────────────────────────────────┐
│ Header: Miyabi Logo | User Avatar | Notifications      │
├──────────┬─────────────────────────────────────────────┤
│          │                                             │
│ Sidebar  │ Main Content                                │
│          │ ┌─────────────────────────────────────────┐ │
│ - Home   │ │ Summary Cards                           │ │
│ - Issues │ │ ┌──────┐ ┌──────┐ ┌──────┐             │ │
│ - Agents │ │ │実行中│ │完了  │ │エラー│             │ │
│ - Flows  │ │ │  3   │ │ 12   │ │  0   │             │ │
│ - Live   │ │ └──────┘ └──────┘ └──────┘             │ │
│ - Settings│ │                                         │ │
│          │ │ Recent Agent Executions                 │ │
│          │ │ ┌─────────────────────────────────────┐ │ │
│          │ │ │ 🤖 CoordinatorAgent | Issue #270    │ │ │
│          │ │ │ ⏱️ 実行中 (5分経過)                 │ │ │
│          │ │ │ [詳細を見る]                        │ │ │
│          │ │ └─────────────────────────────────────┘ │ │
│          │ └─────────────────────────────────────────┘ │
└──────────┴─────────────────────────────────────────────┘
```

**実装コード**:
```typescript
// app/dashboard/layout.tsx
import { Sidebar } from '@/components/layout/sidebar';
import { Header } from '@/components/layout/header';

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="flex h-screen">
      <Sidebar />
      <div className="flex-1 flex flex-col">
        <Header />
        <main className="flex-1 overflow-y-auto p-8 bg-gray-50">
          {children}
        </main>
      </div>
    </div>
  );
}
```

---

### パターン2: Agent選択パレット

**参考**: React Flow Examples

```
┌─────────────────────────────────────────────────────────┐
│ Agent Palette                                    [✕]    │
├─────────────────────────────────────────────────────────┤
│ [検索...]                       [Coding] [Business]     │
├─────────────────────────────────────────────────────────┤
│ 🔧 Coding Agents (7個)                                  │
│                                                         │
│ ┌──────────────────┐ ┌──────────────────┐              │
│ │ 🤖 しきるん       │ │ 🤖 つくるん       │              │
│ │ Coordinator      │ │ CodeGen          │              │
│ │ タスク分解       │ │ コード生成       │              │
│ └──────────────────┘ └──────────────────┘              │
│                                                         │
│ ┌──────────────────┐ ┌──────────────────┐              │
│ │ 🤖 めだまん       │ │ 🤖 はこぶん       │              │
│ │ Review           │ │ Deployment       │              │
│ │ 品質レビュー     │ │ デプロイ         │              │
│ └──────────────────┘ └──────────────────┘              │
└─────────────────────────────────────────────────────────┘
```

**実装コード**:
```typescript
// components/agent/AgentPalette.tsx
import { Card } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { Input } from '@/components/ui/input';

interface Agent {
  type: string;
  displayName: string;
  description: string;
  category: 'coding' | 'business';
  emoji: string;
}

export function AgentPalette({ agents }: { agents: Agent[] }) {
  return (
    <div className="w-[400px] h-screen border-l bg-white p-4">
      <h2 className="text-xl font-bold mb-4">Agent Palette</h2>

      <Input
        placeholder="Agentを検索..."
        className="mb-4"
      />

      <div className="space-y-6">
        <div>
          <h3 className="font-medium mb-2">🔧 Coding Agents</h3>
          <div className="grid grid-cols-2 gap-2">
            {agents.filter(a => a.category === 'coding').map(agent => (
              <Card
                key={agent.type}
                className="p-3 cursor-move hover:shadow-lg transition"
                draggable
              >
                <div className="text-2xl mb-1">{agent.emoji}</div>
                <div className="font-medium text-sm">{agent.displayName}</div>
                <div className="text-xs text-gray-500">{agent.type}</div>
              </Card>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
```

---

### パターン3: Agent実行ステータス

**参考**: Progress + Badge + Card の組み合わせ

```
┌─────────────────────────────────────────────────────────┐
│ Agent実行: Issue #270 - API統合                         │
├─────────────────────────────────────────────────────────┤
│ 🤖 CoordinatorAgent (しきるん)                          │
│ ⏱️ 経過時間: 3分15秒                                     │
│                                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ ✅ Task分解完了 (30秒)                               │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ ⏳ コード生成中... (進行中)                          │ │
│ │ [████████████░░░░░░] 60%                            │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ ⏸️ レビュー待機中                                    │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ [ログを見る] [停止]                                     │
└─────────────────────────────────────────────────────────┘
```

**実装コード**:
```typescript
// components/agent/ExecutionStatus.tsx
import { Card } from '@/components/ui/card';
import { Progress } from '@/components/ui/progress';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';

interface ExecutionStep {
  name: string;
  status: 'completed' | 'running' | 'pending';
  progress?: number;
  duration?: number;
}

export function ExecutionStatus({ steps }: { steps: ExecutionStep[] }) {
  return (
    <Card className="p-6">
      <h2 className="text-2xl font-bold mb-4">
        Agent実行: Issue #270 - API統合
      </h2>

      <div className="space-y-4">
        {steps.map((step, index) => (
          <Card key={index} className="p-4">
            <div className="flex items-center justify-between mb-2">
              <div className="flex items-center gap-2">
                {step.status === 'completed' && <span>✅</span>}
                {step.status === 'running' && <span>⏳</span>}
                {step.status === 'pending' && <span>⏸️</span>}
                <span className="font-medium">{step.name}</span>
              </div>
              {step.duration && (
                <Badge variant="secondary">{step.duration}秒</Badge>
              )}
            </div>

            {step.status === 'running' && step.progress !== undefined && (
              <Progress value={step.progress} className="h-2" />
            )}
          </Card>
        ))}
      </div>

      <div className="flex gap-2 mt-6">
        <Button variant="outline">ログを見る</Button>
        <Button variant="destructive">停止</Button>
      </div>
    </Card>
  );
}
```

---

## 📊 参考にすべきコンポーネント

### 1. データテーブル（Issue一覧）

**参考**: shadcn/ui Data Table Example
**URL**: https://ui.shadcn.com/docs/components/data-table

**機能**:
- ソート
- フィルター
- ページネーション
- 選択機能

---

### 2. グラフ（Recharts）

**参考**: Kiranism/next-shadcn-dashboard-starter

**グラフタイプ**:
- 折れ線グラフ（Agent実行数の推移）
- 棒グラフ（Agent別実行回数）
- 円グラフ（成功/失敗率）

---

### 3. ドラッグ&ドロップ（React Flow）

**参考**: React Flow公式ドキュメント
**URL**: https://reactflow.dev/

**実装すべき機能**:
- ノードのドラッグ&ドロップ
- エッジの接続
- ズーム/パン
- ミニマップ

---

## 🎯 Miyabi UI設計の原則

### 1. シンプルさ優先

**悪い例**:
```typescript
<Card className="p-6 border-2 border-blue-500 shadow-2xl rounded-3xl bg-gradient-to-r from-blue-50 to-purple-50">
  {/* 過剰な装飾 */}
</Card>
```

**良い例**:
```typescript
<Card className="p-6">
  {/* shadcn/uiのデフォルトスタイルを信頼 */}
</Card>
```

---

### 2. 一貫性

**カラースキーム**:
```typescript
const colors = {
  primary: 'blue', // shadcn/uiデフォルト
  success: 'green',
  warning: 'amber',
  error: 'red',
  gray: 'slate',
};
```

**スペーシング**:
- 小: `gap-2` (8px)
- 中: `gap-4` (16px)
- 大: `gap-6` (24px)

---

### 3. レスポンシブ

**ブレークポイント**:
```typescript
// Tailwind CSS デフォルト
sm: '640px',   // スマホ横
md: '768px',   // タブレット縦
lg: '1024px',  // タブレット横/ノートPC
xl: '1280px',  // デスクトップ
'2xl': '1536px', // 大画面
```

**実装例**:
```typescript
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  {/* スマホ: 1列、タブレット: 2列、PC: 3列 */}
</div>
```

---

## 📝 Next Steps

### Week 1-2（Phase 0）

1. [ ] Figmaデザインファイル作成
   - 参考: shadcn-admin, next-shadcn-dashboard-starter
   - カラーパレット: Tailwind Blue系
   - タイポグラフィ: Inter

2. [ ] コンポーネントライブラリセットアップ
   - `npx shadcn-ui@latest init`
   - 必要なコンポーネント追加（Button, Card, Dialog等）

3. [ ] レイアウト実装
   - Sidebar + Header レイアウト
   - ダークモード対応

---

## 📚 参考リンク

- [shadcn/ui公式](https://ui.shadcn.com/)
- [shadcn-admin](https://github.com/satnaing/shadcn-admin)
- [next-shadcn-dashboard-starter](https://github.com/Kiranism/next-shadcn-dashboard-starter)
- [React Flow](https://reactflow.dev/)
- [Recharts](https://recharts.org/)

---

**文責**: Claude Code (AI Assistant)
**作成日**: 2025-10-22
**次回レビュー**: Week 2（デザイン完成時）
