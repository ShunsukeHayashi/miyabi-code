# Automatically export Miyabi environment variables when entering the repo.
# Requires direnv (https://direnv.net/); run `direnv allow` after installation.

if [ -f .env ]; then
  set -a
  # shellcheck source=/dev/null
  source .env
  set +a
fi
